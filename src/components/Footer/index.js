import { Link } from 'gatsby';
import React from 'react';
import './style.scss';

const Footer = ({ author, title }) => (
  <div className="footer">
    <div className="container">
      <p className="text-center text-white">飞升中...</p>
    </div>
  </div>
);

export default Footer;
